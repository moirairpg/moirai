name: Deploy to Digital Ocean
on:
  push:
    tags:
      - '**'
  workflow_dispatch:
jobs:
  MoirAI:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout tag
        uses: actions/checkout@v3
        with:
          fetch-depth: 0
      - name: Extract MoirAI tag version
        run: |
            echo "LATEST_VERSION_TAG=$(git describe --tags --exact-match)" >> $GITHUB_ENV
      - name: Build and push image to GitLab Container Registry
        run: |
          echo ${{ secrets.GITLAB_ACCESS_TOKEN }} | docker login registry.gitlab.com -u thaalesalves --password-stdin
          docker build -t registry.gitlab.com/thaalesalves/moirai-be:${{ env.LATEST_VERSION_TAG }} .
          docker push registry.gitlab.com/thaalesalves/moirai-be:${{ env.LATEST_VERSION_TAG }}
