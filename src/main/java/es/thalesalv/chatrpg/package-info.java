/**
 * This project is structured using the hexagonal architecture, and is broken
 * into layers: adapters, application and domain. Each layer contains its own
 * usage and logic.
 */
package es.thalesalv.chatrpg;